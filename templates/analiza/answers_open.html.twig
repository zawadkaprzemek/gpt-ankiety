<table class="table table-striped table-borderless">
    <thead>
        <tr>
            <th style="width: 70%">Odpowiedź</th>
            <th style="width: 30%;"></th>
        </tr>
    </thead>
    <tbody>
        {% for vote in results.votes %}
            <tr>
                <td>{{ vote.answer|print_answer(question) }}</td>
                <td>{{ 'respondent_'~vote.sessionUser.id }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>